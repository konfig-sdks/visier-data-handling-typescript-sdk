import { VisierDataHandling } from "./index";

describe("visier-data-handling-typescript-sdk", () => {
    it("initialize client", async () => {
        const visierdatahandling = new VisierDataHandling({
        });
    });
});
